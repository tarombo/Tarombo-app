This file documents the text export feature implementation for Diagram.

Changes needed:
1. Add "Text" option to share dialog (line ~159)
2. Add "Text" option to export dialog (line ~174)  
3. Add handler for text export in onActivityResult (after line ~1273)
4. Add shareDiagramAsText() method
5. Add generateFamilyTreeText() method
6. Add string resource "text_exported_ok"

Notes:
- Use context.getString() for resources in non-UI methods
- Java 8 doesn't have String.repeat(), use loop instead
- Use U.epiteto(person) to get person's name
- Use U.twoDates(person, false) to get dates

